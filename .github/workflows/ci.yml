# .github/workflows/ci.yml
name: CI Check

on: [push]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: ls -la
      - run: echo "✅ CI passed for $(git rev-parse --short HEAD)"